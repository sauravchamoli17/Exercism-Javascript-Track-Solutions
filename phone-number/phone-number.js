export const clean = () => {
  
};

// const clean = (number) => {
//    if(number.length < 10)
//     throw new Error('Incorrect number of digits');
//    else if(number.length === 11 && number[0] != 1)
//     throw new Error('11 digits must start with 1');
//    else if(number.length === 11 && /[.,\/#!$%+\^&\*;:{}=\-_`~ ()]/g.test(number[0]))
//     return number.replace(/[^A-z02-9]/g,'');
  
//    // console.log(number.replace(/[^0-9]/g,''));
// };

// clean('+1 (223) 456-7890');